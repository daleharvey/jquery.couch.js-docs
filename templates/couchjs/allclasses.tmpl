<h2>Index</h2>
<ul class="classList">
    <for each="thisClass" in="data">
    <li>{!
        if (thisClass.alias == "_global_") {
          output += "";
        } else {
           output += new Link().toClass(thisClass.alias);
        }
   !}</li>
   </for>
</ul>